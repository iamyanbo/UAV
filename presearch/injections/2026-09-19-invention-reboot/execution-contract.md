# Operator instruction: restore autonomous method invention

The user explicitly authorizes the assistant to intervene as research orchestrator and wants CURI to generate and implement research contributions. The navigation/tracking/exploration taxonomy remains useful, but the prior NAV/TRACK/EXPLORE experiment tree is no longer the deliverable or active backlog. Its scenarios can evaluate proposed methods. Preserve original interests in visual JEV-like decisions, language/geometric JEPA, shared slow/fast capacity, direct geometric/Gaussian state and rapid real-to-sim capture.

These cards are operator-proposed method hypotheses. They are not Astra outputs, published algorithms, certified novel contributions or results. The code in `research/methods_2026_09_19/` is a concrete starting implementation of selected operators. Extend or revise it rather than describing it as a trained UAV system. This finite injection restores the research intent; ordinary continuous Spark orchestration should continue inventing methods after it is consumed.

## What the lead and executor should produce

Produce an evolving method: exact architecture and inference rule, trainable objective where relevant, executable code, an actual learned checkpoint for the learned contribution, and an integrated behavior demonstrating what the addition enables. Tie the residual contribution to primary-source comparisons and make it removable in an ablation. A small pilot can precede the implementation, but return it as an intermediate result with the next substantive implementation step. Method-development completion additionally requires `REPRESENTATIVE_RESULT.md` containing `REPRESENTATIVE_VALIDATION: COMPLETE`, actual RGB/depth/visual-encoder inputs, a rendered 3D or continuous simulated environment where motion advances during inference, end-to-end latency/action-age measurement, mission-level metrics and strong mechanism-specific baselines. Physical flight is not required. Do not silently replace a method-development assignment with a literature-only verdict, a scalar-state classifier or an easier benchmark and then mark the research question complete.

Use `METHOD.md` to state motivation, original-user lineage, closest functional methods, proposed difference, equations/pseudocode, model inputs/outputs, trainable/frozen parameters, data, losses and deployment dependencies. Use `RESULT.md`, `metrics.json`, configuration/seeds, source references, code identity, commands/exit codes, raw episode results and checkpoint identity for evidence. Explain what the method does in ordinary language. Existing algebra or components can underpin a nontrivial method improvement; discovering component overlap should refine the invention, not automatically turn the task into replication.

Ordinary offline/simulation implementation is authorized. After the invalid September 19 Method 1 pilot, each method-development task requires one explicit lead design review before execution and a returned-code/metrics audit before an outcome. Review the deployed information path, privileged training-only data, slow-path timing, environment, closest prior art, baselines and mission discrimination. An invalid pilot may close as inconclusive or blocked, but it cannot support or refute the proposed method. This is a scientific-fidelity check, not a publication-readiness, source-count or novelty-certificate gate. If the precise residual is already implemented, formulate an informed alternative or close that method with a concrete reason and continue another ready direction. Do not force an invention when the evidence leaves none. The lead may generate proposals outside these three; mission categories are application labels, not a quota requiring one paper per category.

## Available compute and implementation scope

Spark remains the reasoning provider. Local experimental hardware is an 8 GiB RTX 3060 Ti with the user's approximately 60% VRAM preference (about 4.8 GiB). Check total usage; an allocator fraction <=0.6 limits one framework/process only. Reduce batch/model/resolution or use CPU rather than kill unrelated processes. Do not assume the Spark GPU is allocated for experiments. Start with compact trainable recurrent/predictive models and available frozen visual features; inspect the already provisioned Qwen/OpenFly path before downloading more weights. Measure the complete encoder+model+planner cost. Cached features are useful training inputs, but live visual latency must eventually include extraction.

A small faithful learned architecture is acceptable; no full VLM or video-world-model pretraining is implied. Geometric tokens, low-resolution RGB-D or modest procedural scenes can make the contribution tractable. A reduced pilot must retain the proposed information flow/objective, and identify what remains for full 3D flight. Resource limits guide architecture design and staged evidence; they are not a reason to replace invention with repeated simple baselines.

## Evidence boundaries

Keep actual observations, training-only simulator labels, model-generated predictions and oracle upper bounds distinct in code. The agent has no hidden true map, future target trajectory or actual outcome at decision time. Offline teacher replay in Method 1 can use events already received by the arrival time, never later events. Method 2 branches are possible evidence; only an actual observation can select a deployed posterior. Method 3's scene generator may see simulator truth during training, but its student and reference policy must share the declared observation budget. Real-to-sim appearance is not validated collision geometry.

Use continuous simulated time while inference runs, bounded sensing, measured action age, speed and braking/turn dynamics. Compare task completion/progress, correct-target persistence or trustworthy exploration at matched compute and sensing. Include meaningful baselines, held-out world families and mechanism ablations. Correct information-flow tests are implementation evidence; they establish neither novelty nor a navigation improvement. Separate incomplete implementation, invalid/non-discriminating pilot, negative result and promising result. Preserve the earlier audit of misleading class imbalance, mismatched capacity and oracle acquisition instead of treating old headline metrics as established facts.

No autonomous physical flight is authorized. Current method development and training can proceed in offline data and simulation. Do not turn that existing boundary into additional approval loops for local work.
