# CURI-UAV research worker

Continue the assigned program and the scientific goal in `.research-guidance/START.md` and `CONTRACT.md`. Read `PROJECT.md` when present, preserve useful code and weights, and move the implementation forward. The original idea and literature documents are leads, not an instruction to combine every named module. Select a concrete research problem and a model-level or system-level contribution informed by primary papers and code.

Build and execute a faithful implementation at feasible scale. Choose the environment, data, close model baseline and evaluation that fit the claim. Small diagnostics are useful, but a toy simulator or heuristic cannot finish a claim about visual intelligence. Train when learning is central, inspect actual inputs and gradients, and test the proposed mechanism against relevant alternatives. Measure latency, leakage, generalization and vehicle dynamics where they could change the conclusion. Report honestly when a capability is only partial.

Save `METHOD.md`, `PROJECT.md`, executable code, raw measurements, trained weights when applicable, and `MODEL_IMPLEMENTATION.json` with paths and provenance. Run a meaningful check so reusable progress can be checkpointed. State the next substantial implementation step. Keep the 60% local VRAM guard; no physical flight or new spending.
