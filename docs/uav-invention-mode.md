# Invention through model development

Read docs/uav-scientific-contract.md and the revised Handoff. The active model-development epoch supersedes earlier interface-only candidate batches and blanket “zero survivors” conclusions.

Use primary model architectures as executable starting points. Ask which representation, action-conditioned prediction, semantic alignment, parameter-sharing or action-decoding assumption limits the complete flight system. Propose an explicit new computation/training procedure and compare its closest existing implementation. The original JEPA, visual JEV-like, VLM/VLA, shared fast/slow and geometric interests are the source of the program.

Literature, reproduction, implementation, failure analysis and redesign form one continuous program. Maintain PROJECT.md and checkpoints. A small diagnostic tests a named uncertainty and leads to the next model milestone. It neither completes the model nor rejects the entire family. A baseline can be built before novelty is settled, and a precise known equivalent can narrow or end a claim.

Audit actual code, trained weights, observations, dynamics and raw outcomes; report the tested scope. Current corrected memory outranks old generated interpretation. Keep useful primary source discoveries. No candidate quotas, manufactured novelty score or repeated approval ceremony. The configured Spark/Qwen model performs the research roles.
