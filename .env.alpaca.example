# Copy to .env.alpaca. Generate these from your PAPER account at app.alpaca.markets.
# Only the broker process reads this file; keep it out of git and model context.
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_API_BASE_URL=https://paper-api.alpaca.markets
ALPACA_DATA_FEED=iex
ALPACA_PAPER_CAPITAL=10000
